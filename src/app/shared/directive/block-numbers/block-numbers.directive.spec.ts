import { BlockNumbersDirective } from './block-numbers.directive';

describe('BlockNumbersDirective', () => {
  it('should create an instance', () => {
    const directive = new BlockNumbersDirective();
    expect(directive).toBeTruthy();
  });
});
