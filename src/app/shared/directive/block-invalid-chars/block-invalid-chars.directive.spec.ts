import { BlockInvalidCharsDirective } from './block-invalid-chars.directive';

describe('BlockInvalidCharsDirective', () => {
  it('should create an instance', () => {
    const directive = new BlockInvalidCharsDirective();
    expect(directive).toBeTruthy();
  });
});
